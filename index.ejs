<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>techtab</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <%- include('public/partials/header.ejs') %>

    <h1 class="title">techtab</h1>
    <div class="form-container">
      <% if (locals.isEdit) { %>
      <p class="form-text">Edit your Blog Post</p>
      <% }else{ %>
      <p class="form-text">Create your Blog Post</p>
      <% } %>

      <form
        action="<%= locals.isEdit ? `/edit/${locals.blogDetails?.id}` : '/home' %>"
        method="post"
      >
        <label for="bTitle">Blog Title</label>
        <input
          type="text"
          id="bTitle"
          name="blogTitle"
          placeholder="Your Blog Title Here..."
          required
        />

        <label for="bDes">Blog description</label>
        <textarea
          type="text"
          id="bDes"
          name="blogDes"
          placeholder="Your Blog Content here..."
          rows="6"
          required
        >
        </textarea>
        <label for="bTitle">Blog Author</label>
        <input
          type="text"
          id="bAuthor"
          name="blogAuthor"
          placeholder="The Blog Author Here..."
        />
        <input
          type="submit"
          value="<%= locals.isEdit ? 'EDIT' : 'PUBLISH' %>"
        />
      </form>
    </div>
    <%- include('public/partials/footer.ejs') %>
  </body>
</html>
